# Field Service Management Frontend

## Requirements
- Node.js

## Run
```bash
npm install
npm run dev
```

Open the Vite local URL shown in terminal. 
Default backend base URL is configured in `.env`.
