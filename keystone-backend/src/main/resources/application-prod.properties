# Production profile — explicit baseline-on-migrate=false to prevent accidental auto-baseline.
# flyway_schema_history must already exist (created by the baseline migration in WO-041)
# before deploying to a production database.
spring.flyway.baseline-on-migrate=false
