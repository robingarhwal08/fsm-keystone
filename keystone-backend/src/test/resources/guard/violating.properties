# Synthetic test fixture — violating case.
# Contains a required-no-default key with a literal fallback.
# Used by DefaultValueGuardTest to verify the guard flags this violation.
# IMPORTANT: the value here is a placeholder only. Never put a real secret here.

my.api.secret=${MY_API_SECRET:placeholder-literal-value-not-a-real-secret}
my.public.setting=${MY_PUBLIC_SETTING:some-safe-default}
