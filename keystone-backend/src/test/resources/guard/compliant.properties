# Synthetic test fixture — compliant case.
# Contains a required-no-default key WITHOUT a literal fallback.
# Used by DefaultValueGuardTest to verify the guard passes this configuration.

my.api.secret=${MY_API_SECRET}
my.public.setting=${MY_PUBLIC_SETTING:some-safe-default}
