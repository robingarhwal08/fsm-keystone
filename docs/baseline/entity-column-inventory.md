# Entity Column Inventory — fsm-keystone

**Source:** `com.fsm.keystone.entity.*` (Spring Boot 4.0.7 / Hibernate 7 / PostgreSQL 16)
**Generated:** 2026-08-10 via entity-class analysis; validated by `EntityInventoryCoverageTest`.

> Note: the work order referenced "18 entities"; the actual model contains **8 @Entity classes**
> and 4 enum types. All 8 entities are enumerated below.

---

## Entities

### AppUser → `users`

| Java field | Column | Java type | PG type | Nullable | Constraint | Lifecycle | Classification |
|---|---|---|---|---|---|---|---|
| `id` | `id` | `Long` | `BIGINT GENERATED BY DEFAULT AS IDENTITY` | NO (PK) | PRIMARY KEY | DB auto-assign | — |
| `fullName` | `full_name` | `String` | `VARCHAR(255)` | NO | — | app-set on create | Personal data |
| `email` | `email` | `String` | `VARCHAR(255)` | NO | UNIQUE | app-set on create | Personal data |
| `password` | `password` | `String` | `VARCHAR(255)` | NO | — | app-set on create (bcrypt hash) | Restricted |
| `phone` | `phone` | `String` | `VARCHAR(255)` | YES | — | app-set | Personal data |
| `role` | `role` | `Role` (enum) | `VARCHAR(255)` | NO | — | app-set on create | — |
| `active` | `active` | `Boolean` | `BOOLEAN` | YES | — | `@PrePersist` defaults to `true` | — |
| `createdAt` | `created_at` | `LocalDateTime` | `TIMESTAMP(6)` | YES | — | `@PrePersist` sets `LocalDateTime.now()` | — |
| `customer` | `customer_id` | `Customer` FK | `BIGINT` | YES | FK → `customers.id` | app-set | — |

**Relationships:**
- `@ManyToOne(fetch = EAGER, optional = true)` → `Customer`; FK `customer_id` nullable
- Implements `UserDetails`; `@JsonIgnore` on `password` suppresses serialization

---

### Customer → `customers`

| Java field | Column | Java type | PG type | Nullable | Constraint | Lifecycle | Classification |
|---|---|---|---|---|---|---|---|
| `id` | `id` | `Long` | `BIGINT GENERATED BY DEFAULT AS IDENTITY` | NO (PK) | PRIMARY KEY | DB auto-assign | — |
| `name` | `name` | `String` | `VARCHAR(255)` | NO | — | app-set | — |
| `email` | `email` | `String` | `VARCHAR(255)` | YES | UNIQUE | app-set | Personal data |
| `phone` | `phone` | `String` | `VARCHAR(255)` | YES | — | app-set | Personal data |
| `billingAddress` | `billing_address` | `String` | `VARCHAR(255)` | YES | — | app-set | Personal data |
| `createdAt` | `created_at` | `LocalDateTime` | `TIMESTAMP(6)` | YES | — | `@PrePersist` sets `LocalDateTime.now()` | — |
| `updatedAt` | `updated_at` | `LocalDateTime` | `TIMESTAMP(6)` | YES | — | `@PrePersist` and `@PreUpdate` sets `LocalDateTime.now()` | — |

**Relationships:**
- `@OneToMany(mappedBy="customer", cascade=ALL, orphanRemoval=true, fetch=LAZY)` → `List<Site>` (`@JsonIgnore`)
- `@OneToMany(mappedBy="customer", fetch=LAZY)` → `List<WorkOrder>` (`@JsonIgnore`)

---

### Site → `sites`

| Java field | Column | Java type | PG type | Nullable | Constraint | Lifecycle | Classification |
|---|---|---|---|---|---|---|---|
| `id` | `id` | `Long` | `BIGINT GENERATED BY DEFAULT AS IDENTITY` | NO (PK) | PRIMARY KEY | DB auto-assign | — |
| `siteName` | `site_name` | `String` | `VARCHAR(255)` | NO | — | app-set | — |
| `address` | `address` | `String` | `VARCHAR(255)` | YES | — | app-set | **Personal data** |
| `city` | `city` | `String` | `VARCHAR(255)` | YES | — | app-set | **Personal data** |
| `state` | `state` | `String` | `VARCHAR(255)` | YES | — | app-set | **Personal data** |
| `pincode` | `pincode` | `String` | `VARCHAR(255)` | YES | — | app-set | **Personal data** |
| `latitude` | `latitude` | `Double` | `FLOAT(53)` | YES | — | app-set | **Personal data** |
| `longitude` | `longitude` | `Double` | `FLOAT(53)` | YES | — | app-set | **Personal data** |
| `contactPerson` | `contact_person` | `String` | `VARCHAR(255)` | YES | — | app-set | **Personal data** |
| `contactPhone` | `contact_phone` | `String` | `VARCHAR(255)` | YES | — | app-set | **Personal data** |
| `customer` | `customer_id` | `Customer` FK | `BIGINT` | NO | FK → `customers.id` | app-set | — |

> **Data-classification finding:** `Site` carries substantially more personal data than typical business entities —
> address, city, state, pincode, latitude, longitude, contactPerson, contactPhone are all PII.
> These fields require masking in logs and anonymisation in non-production environments.

**Relationships:**
- `@ManyToOne(fetch=EAGER, optional=false)` → `Customer`; FK `customer_id` NOT NULL; cascade-deleted when customer is deleted

---

### Part → `parts`

| Java field | Column | Java type | PG type | Nullable | Constraint | Lifecycle | Classification |
|---|---|---|---|---|---|---|---|
| `id` | `id` | `Long` | `BIGINT GENERATED BY DEFAULT AS IDENTITY` | NO (PK) | PRIMARY KEY | DB auto-assign | — |
| `partName` | `part_name` | `String` | `VARCHAR(255)` | NO | — | app-set | — |
| `partNumber` | `part_number` | `String` | `VARCHAR(255)` | NO | UNIQUE | app-set | — |
| `description` | `description` | `String` | `VARCHAR(255)` | YES | — | app-set | — |
| `unitPrice` | `unit_price` | `BigDecimal` | `NUMERIC(38, 2)` | YES | — | app-set | — |
| `stockQuantity` | `stock_quantity` | `Integer` | `INTEGER` | YES | — | `@PrePersist` defaults to `0` | — |
| `active` | `active` | `Boolean` | `BOOLEAN` | YES | — | `@PrePersist` defaults to `true` | — |

> **Anomaly — missing reorderLevel and supplier (Finding A):**
> `Part` has no `reorderLevel` or `supplier` column. The low-stock dashboard threshold is
> hardcoded as `5` in `DashboardService.java:58` (`partRepo.countByStockQuantityLessThanEqual(5)`).
> Adding `reorderLevel` per part would require a schema migration.

---

### WorkOrder → `work_orders`

| Java field | Column | Java type | PG type | Nullable | Constraint | Lifecycle | Classification |
|---|---|---|---|---|---|---|---|
| `id` | `id` | `Long` | `BIGINT GENERATED BY DEFAULT AS IDENTITY` | NO (PK) | PRIMARY KEY | DB auto-assign | — |
| `workOrderNumber` | `work_order_number` | `String` | `VARCHAR(255)` | NO | UNIQUE | `@PrePersist` sets `"WO-" + System.currentTimeMillis()` if null | — |
| `title` | `title` | `String` | `VARCHAR(255)` | NO | — | app-set | — |
| `description` | `description` | `String` | `VARCHAR(2000)` | YES | — | app-set | — |
| `status` | `status` | `WorkOrderStatus` (enum) | `VARCHAR(255)` | YES | — | `@PrePersist` defaults to `CREATED` | — |
| `priority` | `priority` | `Priority` (enum) | `VARCHAR(255)` | YES | — | `@PrePersist` defaults to `MEDIUM` | — |
| `scheduledStart` | `scheduled_start` | `LocalDateTime` | `TIMESTAMP(6)` | YES | — | app-set | — |
| `scheduledEnd` | `scheduled_end` | `LocalDateTime` | `TIMESTAMP(6)` | YES | — | app-set | — |
| `actualStart` | `actual_start` | `LocalDateTime` | `TIMESTAMP(6)` | YES | — | app-set | — |
| `actualEnd` | `actual_end` | `LocalDateTime` | `TIMESTAMP(6)` | YES | — | app-set | — |
| `createdAt` | `created_at` | `LocalDateTime` | `TIMESTAMP(6)` | YES | — | `@PrePersist` sets `LocalDateTime.now()` | — |
| `updatedAt` | `updated_at` | `LocalDateTime` | `TIMESTAMP(6)` | YES | — | `@PrePersist` and `@PreUpdate` set `LocalDateTime.now()` | — |
| `customer` | `customer_id` | `Customer` FK | `BIGINT` | NO | FK → `customers.id` | app-set | — |
| `site` | `site_id` | `Site` FK | `BIGINT` | NO | FK → `sites.id` | app-set | — |
| `createdBy` | `created_by_user_id` | `AppUser` FK | `BIGINT` | YES | FK → `users.id` | app-set | — |
| `assignedTechnician` | `assigned_technician_id` | `AppUser` FK | `BIGINT` | YES | FK → `users.id` | app-set | — |

> **Anomaly — undocumented actualStart / actualEnd (Finding B):**
> `actual_start` and `actual_end` are present in the schema but have no API surface for setting them
> via the request DTOs. They exist as database columns populated only via direct entity manipulation.
>
> **Anomaly — timestamp-based work order number generator (Finding C):**
> `workOrderNumber` defaults to `"WO-" + System.currentTimeMillis()` in `@PrePersist`
> (`WorkOrder.java:88`). Under concurrent inserts within the same millisecond, two work orders
> could receive the same number, violating the UNIQUE constraint and causing a constraint violation
> at commit time. See `WorkOrderService.java` for the caller.

**Relationships:**
- `@ManyToOne(fetch=EAGER, optional=false)` → `Customer`; FK `customer_id` NOT NULL
- `@ManyToOne(fetch=EAGER, optional=false)` → `Site`; FK `site_id` NOT NULL
- `@ManyToOne(fetch=EAGER)` → `AppUser` as `createdBy`; FK `created_by_user_id` nullable
- `@ManyToOne(fetch=EAGER)` → `AppUser` as `assignedTechnician`; FK `assigned_technician_id` nullable
- `@OneToMany(mappedBy="workOrder", cascade=ALL, fetch=LAZY)` → `List<PartUsage>` (`@JsonIgnore`)
- `@OneToMany(mappedBy="workOrder", cascade=ALL, fetch=LAZY)` → `List<TimeLog>` (`@JsonIgnore`)
- `@OneToMany(mappedBy="workOrder", cascade=ALL, fetch=LAZY)` → `List<StatusHistory>` (`@JsonIgnore`)

---

### PartUsage → `part_usage`

| Java field | Column | Java type | PG type | Nullable | Constraint | Lifecycle | Classification |
|---|---|---|---|---|---|---|---|
| `id` | `id` | `Long` | `BIGINT GENERATED BY DEFAULT AS IDENTITY` | NO (PK) | PRIMARY KEY | DB auto-assign | — |
| `quantityUsed` | `quantity_used` | `Integer` | `INTEGER` | YES | — | app-set | — |
| `unitPriceAtUsage` | `unit_price_at_usage` | `BigDecimal` | `NUMERIC(38, 2)` | YES | — | app-set | — |
| `totalCost` | `total_cost` | `BigDecimal` | `NUMERIC(38, 2)` | YES | — | app-set | — |
| `usedAt` | `used_at` | `LocalDateTime` | `TIMESTAMP(6)` | YES | — | `@PrePersist` sets `LocalDateTime.now()` | — |
| `workOrder` | `work_order_id` | `WorkOrder` FK | `BIGINT` | NO | FK → `work_orders.id` | app-set | — |
| `part` | `part_id` | `Part` FK | `BIGINT` | NO | FK → `parts.id` | app-set | — |
| `usedBy` | `used_by_user_id` | `AppUser` FK | `BIGINT` | YES | FK → `users.id` | app-set | — |

**Relationships:**
- `@ManyToOne(fetch=EAGER, optional=false)` → `WorkOrder`; FK `work_order_id` NOT NULL
- `@ManyToOne(fetch=EAGER, optional=false)` → `Part`; FK `part_id` NOT NULL
- `@ManyToOne(fetch=EAGER)` → `AppUser` as `usedBy`; FK `used_by_user_id` nullable

---

### TimeLog → `time_logs`

| Java field | Column | Java type | PG type | Nullable | Constraint | Lifecycle | Classification |
|---|---|---|---|---|---|---|---|
| `id` | `id` | `Long` | `BIGINT GENERATED BY DEFAULT AS IDENTITY` | NO (PK) | PRIMARY KEY | DB auto-assign | — |
| `startTime` | `start_time` | `LocalDateTime` | `TIMESTAMP(6)` | YES | — | app-set | — |
| `endTime` | `end_time` | `LocalDateTime` | `TIMESTAMP(6)` | YES | — | app-set | — |
| `hoursSpent` | `hours_spent` | `BigDecimal` | `NUMERIC(38, 2)` | YES | — | app-set | — |
| `workDescription` | `work_description` | `String` | `VARCHAR(1000)` | YES | — | app-set | — |
| `workOrder` | `work_order_id` | `WorkOrder` FK | `BIGINT` | NO | FK → `work_orders.id` | app-set | — |
| `technician` | `technician_id` | `AppUser` FK | `BIGINT` | NO | FK → `users.id` | app-set | — |

**Relationships:**
- `@ManyToOne(fetch=EAGER, optional=false)` → `WorkOrder`; FK `work_order_id` NOT NULL
- `@ManyToOne(fetch=EAGER, optional=false)` → `AppUser` as `technician`; FK `technician_id` NOT NULL

---

### StatusHistory → `status_history`

| Java field | Column | Java type | PG type | Nullable | Constraint | Lifecycle | Classification |
|---|---|---|---|---|---|---|---|
| `id` | `id` | `Long` | `BIGINT GENERATED BY DEFAULT AS IDENTITY` | NO (PK) | PRIMARY KEY | DB auto-assign | — |
| `oldStatus` | `old_status` | `WorkOrderStatus` (enum) | `VARCHAR(255)` | YES | — | app-set | — |
| `newStatus` | `new_status` | `WorkOrderStatus` (enum) | `VARCHAR(255)` | YES | — | app-set | — |
| `comment` | `comment` | `String` | `VARCHAR(255)` | YES | — | app-set | — |
| `changedAt` | `changed_at` | `LocalDateTime` | `TIMESTAMP(6)` | YES | — | `@PrePersist` sets `LocalDateTime.now()` | — |
| `workOrder` | `work_order_id` | `WorkOrder` FK | `BIGINT` | NO | FK → `work_orders.id` | app-set | — |
| `changedBy` | `changed_by_user_id` | `AppUser` FK | `BIGINT` | YES | FK → `users.id` | app-set | — |

**Relationships:**
- `@ManyToOne(fetch=EAGER, optional=false)` → `WorkOrder`; FK `work_order_id` NOT NULL
- `@ManyToOne(fetch=EAGER)` → `AppUser` as `changedBy`; FK `changed_by_user_id` nullable

---

## Enums (not persisted as separate tables)

| Enum | Values | Persisted as |
|---|---|---|
| `Role` | `MANAGER`, `DISPATCHER`, `TECHNICIAN`, `CUSTOMER` | `VARCHAR(255)` via `@Enumerated(STRING)` in `users.role` |
| `WorkOrderStatus` | `CREATED`, `ASSIGNED`, `IN_PROGRESS`, `ON_HOLD`, `COMPLETED`, `CLOSED`, `CANCELLED` | `VARCHAR(255)` in `work_orders.status`, `status_history.old_status`, `status_history.new_status` |
| `Priority` | `LOW`, `MEDIUM`, `HIGH`, `CRITICAL` | `VARCHAR(255)` in `work_orders.priority` |
| `Permission` | `CUSTOMER_CREATE`, `CUSTOMER_UPDATE`, … (13 values) | Not persisted — used only in application logic; no column |

---

## Relationship Map

```
Customer 1 ──────────────────────── N Site
Customer 1 ──────────────────────── N WorkOrder
Site      1 ──────────────────────── N WorkOrder

AppUser (MANAGER/DISPATCHER) 1 ── N WorkOrder (as createdBy)
AppUser (TECHNICIAN)         1 ── N WorkOrder (as assignedTechnician)
AppUser (TECHNICIAN)         1 ── N TimeLog   (as technician)
AppUser                      1 ── N PartUsage (as usedBy)
AppUser                      1 ── N StatusHistory (as changedBy)

WorkOrder 1 ──────────────────────── N PartUsage
WorkOrder 1 ──────────────────────── N TimeLog
WorkOrder 1 ──────────────────────── N StatusHistory

Part 1 ──────────────────────────── N PartUsage
```

`@JsonIgnore` annotations on `Customer.sites`, `Customer.workOrders`,
`WorkOrder.partUsages`, `WorkOrder.timeLogs`, `WorkOrder.statusHistory`
prevent runaway serialization on bidirectional associations.

---

## Documented Anomalies

| ID | Finding | Source reference | Flyway epic action |
|---|---|---|---|
| A | `Part` has no `reorderLevel` or `supplier` column; low-stock threshold is hardcoded as `5` | `DashboardService.java:58`, `Part.java` | Add `reorder_level INTEGER` column; parameterise threshold |
| B | `WorkOrder.actualStart` and `actualEnd` exist as columns but have no DTO surface or API endpoint for setting them | `WorkOrder.java:44-45`; no corresponding field in `CreateWorkOrderRequest.java` | Add API field or remove columns in a squash migration |
| C | `workOrderNumber` generated via `"WO-" + System.currentTimeMillis()` creates collision risk under concurrent inserts | `WorkOrder.java:88` | Replace with a database sequence-based generator |
| D | `Site` holds substantial personal data (8 PII fields) with no masking or anonymization logic | `Site.java` | Implement DTO-level masking; anonymize non-prod datasets |
